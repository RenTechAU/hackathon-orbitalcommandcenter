# web/

Dashboard UI lands here.

Generate it with the prompt in `../docs/CLAUDE_DESIGN_BRIEF.md`, save the
result as `index.html`, and open it directly in a browser — no server needed.

Do this around 3 PM, after the backend works.
